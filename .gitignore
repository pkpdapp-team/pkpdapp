# Byte-compiled / optimized / DLL files
__pycache__/

# Virtual environments
venv
.venv
env*

# Setuptools files
*.egg-info

# database file (not sure yet what its good for)
db.sqlite3

# DS
.DS_Store

# static files
pkpdapp/static

# nodemodules
frontend/node_modules

logfile.log*
chatbot.log*

.vscode

notes.md
AGENTS.md
agent_plan
tmp/
.claude
.devcontainer
pkpdapp/pkpdapp/tests/test_models/optimise_verification_plots
# TLS certs (private keys) and the ACME challenge webroot — runtime/secret, never commit
.certs/
certbot-webroot/

# uv / npm — not used (repo uses pip + yarn)
/pyproject.toml
/uv.lock
/.python-version
/frontend-v2/package-lock.json
